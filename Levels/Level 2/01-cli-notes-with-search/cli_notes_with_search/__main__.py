"""
Entry point for running the CLI Notes with Search application as a module.

This allows the package to be executed with:
    python -m cli_notes_with_search
"""

from .main import cli

if __name__ == "__main__":
    cli()
